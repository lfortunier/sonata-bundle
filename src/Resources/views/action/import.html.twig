{% if admin.isGranted('CREATE') and admin.isGranted('EDIT') %}
    <li>
        <a class="sonata-action-element" href="{{ admin.generateUrl('import') }}"><i class="fa fa-upload" aria-hidden="true"></i>
            {{ 'action.import'|trans({}, 'admin') }}
        </a>
    </li>
{% endif %}
